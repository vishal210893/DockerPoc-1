<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- =================================================================== -->
    <!--                               Appenders                             -->
    <!-- =================================================================== -->

    <!-- Console Appender: always defined; pattern includes colour codes for terminal output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--
                Pattern explanation:
                %gray(%d{yyyy-MM-dd HH:mm:ss.SSS}) : Timestamp in gray
                %highlight(%-5level)               : Log level, coloured by severity
                %magenta([%thread])                : Thread name in magenta brackets
                %blue(%logger)                     : Logger (class) name in blue
                %yellow(%M:%L)                     : Method and line number in yellow
                %msg%n                             : The log message and newline
                %throwable                         : Stack trace when present

                WARNING: %M / %L are expensive; avoid in high-volume production loggers.
            -->
            <pattern>
                [%boldGreen(%d{yyyy-MM-dd HH:mm:ss.SSS})] %highlight(%-5level) %magenta([%thread]) %cyan(%logger) [%boldYellow(%M:%L)] : %msg%n%throwable
            </pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender: only defined under the 'prod' profile. -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/dockerpoc.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>logs/dockerpoc-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxHistory>30</maxHistory>
                <maxFileSize>100MB</maxFileSize>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>
                    %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} : %msg%n%throwable
                </pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- =================================================================== -->
    <!--                               Loggers                               -->
    <!-- =================================================================== -->
    <!--
        Spring Boot requires <springProfile> to wrap whole <root>/<logger> elements,
        not be nested inside them. So we duplicate the blocks per profile.
    -->

    <!-- Non-prod (dev, default, test, …): CONSOLE only -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.learning.docker" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <!-- prod: CONSOLE + rolling FILE -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.learning.docker" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>

    <!-- Example: trim chatty third-party loggers
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>
    -->

</configuration>
